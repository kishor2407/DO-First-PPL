pipeline
{
    agent any
    stages {
           stage('Print_Message')
               { steps{ sh 'echo hello_world'} }
            satge('Build')
                {steps{ sh echo Build_successful'}}
    }
}
